<div class="container">
    <div class="row">
        <div class="col-8 offset-2">
            <div class="card mt-4">
                <div class="card-body">
                    <h5 class="card-title">Projects</h5>
                    <ul id="allProject" class="list-group">
                        {{#each Project}}
                        <li data-id="{{this.id}}" class="list-group-item">
                            {{!-- <a href="user/{{this.id}}">{{this.name}}</a> --}}
                            <h4>{{this.project_Name}}</h4>
                            <p>Created by: User #{{this.UserId}}</p>
                            <button class="btn btn-danger float-right delete">ｘ</button>
                        </li>
                        {{/each}}
                    </ul>
                </div>
                {{!-- the commented out code below is a form we can use to create a new user. We should be using it at some point. --}}
                <div class="card-body">
                    <form class="create-form">

                        <div class="form-group">
                            <label for="newProject">Project Name:</label>
                            <input type="text" id="newProject" name="name">
                        </div>

                        <button type="submit">Add Project</button>
                        
                </div>
                <div>
                    <a href="/">← Back To Home</a>
                </div>
            </div>
        </div>
    </div>
</div>